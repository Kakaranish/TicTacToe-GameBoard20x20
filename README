1. Compile Server
javac Server.java

2. Compile Client
javac Client.java

3. Run Server
java -classpath ".;./hsqldb.jar" Server

4. Run two clients
java Client

If there's a game that was interrupted before, then server will load it and show players the course of game. 